query GetClients {
  clients {
    id
    displayName
    normalizedCacheInfos {
      id
      displayName
      recordCount
    }
  }
}

query GetNormalizedCache($id: ID!){
  normalizedCache(id: $id) {
    id
    displayName
    clientDisplayName
    records {
      key
      record
    }
  }
}
